import findFreeSlots from "./src/utils/findFreeSlots"
import parseTimetable from "./src/utils/parseTimeTable"

const s = ``

console.log(findFreeSlots([s]))

export { findFreeSlots, parseTimetable }
export default findFreeSlots
