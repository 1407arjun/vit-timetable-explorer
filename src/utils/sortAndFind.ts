const sortAndFind = (timings: string[][]) => {
    const res = []

    timings.sort()

    const temp = []

    while (timings.length > 0) {
        const pair = timings.pop()

        if (temp && temp[temp.length - 1][1])
    }
}

export default sortAndFind
